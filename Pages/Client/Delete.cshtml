@page
@using System.Data.SqlClient;
@{
    try
    {
        String id = Request.Query["id"];
        String ConnectionString = "Data Source=DESKTOP-F12I18V\\SQLEXPRESS01;Initial Catalog=MYSTORE;Integrated Security=True";

        using (SqlConnection connection = new SqlConnection(ConnectionString))
        {
            connection.Open();
            String sql = "delete from client where id=@id";
            using (SqlCommand commond = new SqlCommand(sql,connection))
            {
                commond.Parameters.AddWithValue("@id", id);
                commond.ExecuteNonQuery();
            }
        }
    }
    catch (Exception e)
    {

    }
    Response.Redirect("/Client/Index");
}
